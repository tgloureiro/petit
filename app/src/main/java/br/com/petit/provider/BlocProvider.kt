package br.com.petit.provider

class BlocProvider{
    /*fun main(): MainScreenLiveBlocFactory
    fun detail(): MyDetailScreenBlocFactory*/

    /*
    fun getLiveBloc(route: Route, factory: LiveBlocFactory): Bloc<*, *> {

        var bloc = stack.current().bloc
        if (bloc == null) {
            Logger.getLogger("Navigator").log(
                Level.WARNING, "Created: Bloc for " + stack.current().route.javaClass.name
            )
            bloc = factory.create(route)
            stack.current().bloc = bloc
        }
        return bloc
    }

     */
}